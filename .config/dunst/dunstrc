[global]
    ### Display ###
    monitor = 0
    follow = keyboard
    # Dimension
    width = 320
    height = 150
    origin = top-right
    offset = 20x20
    # Progress Bar
    progress_bar = true
    progress_bar_height = 10
    progress_bar_frame_width = 2
    progress_bar_min_width = 0
    progress_bar_max_width = 320
    # Padding
    padding = 14
    horizontal_padding = 14
    text_icon_padding = 18
    # Other
    frame_width = 1
    frame_color = "#89B4FA""
    separator_height = 6
    gap_size = 8
    notification_limit = 3
    indicate_hidden = true
    sort = true
    idle_threshold = 3m

    ### Text ###
    font = JetBrainsMono Nerd Font 10
    line_height = 0
    markup = full
    alignment = left
    vertical_alignment = center
    show_age_threshold = 15m
    word_wrap = true
    ellipsize = middle
    ignore_newline = false
    stack_duplicates = true
    hide_duplicate_count = false
    show_indicators = true

    ### Icons ###
    icon_position = left
    enable_recursive_icon_lookup = true
    icon_theme = "Papirus, Adwaita, breeze"
    min_icon_size = 48
    max_icon_size = 64

    ### History ###
    history_length = 10

    ### External ###
    # dmenu = "/usr/bin/rofi -markup -dmenu -config '~/.config/rofi/theme.rasi'"
    browser = "/usr/bin/xdg-open"
    corner_radius = 5
    ignore_dbusclose = false

    ### Wayland ###
    layer = overlay

    ### Mouse ###
    mouse_left_click = close_current
    mouse_middle_click = close_all
    mouse_right_click = do_action, close_current

    ## Catppuccin Color scheme
    [urgency_low]
    background = "#1E1E2E"
    foreground = "#CDD6F4"
    
    [urgency_normal]
    background = "#1E1E2E"
    foreground = "#CDD6F4"
    
    [urgency_critical]
    background = "#1E1E2E"
    foreground = "#CDD6F4"
    frame_color = "#FAB387"

    # Custom Rules
    [fullscreen_delay_everything]
        fullscreen = pushback
    [fullscreen_show_critical]
        msg_urgency = critical
        fullscreen = show
    [spotify]
        desktop_entry = "Spotify"
        timeout = 3
        history_ignore = yes
    [system_info]
        appname = "system_info"
        urgency = low
        history_ignore = yes
        timeout = 3
        fullscreen = show

    # vim: ft=cfg
